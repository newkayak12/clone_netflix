version: '3'
services:
  rabbit:
    container_name: RabbitMQ
    build:
      context: .
    image: rabbitmq:3.10
    restart: unless-stopped
    environment:
      RABBITMQ_DEFAULT_USER: root
      RABBITMQ_DEFAULT_PASS: root
    ports:
      - 5672:5672
      - 15672:15672

